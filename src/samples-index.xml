<?xml version="1.0" encoding="UTF-8"?>
<samples xmlns="http://www.daisy.org/ns/daisy-online/#" xmlns:db="http://docbook.org/ns/docbook">

  <!-- a list that the DaisyOnline_xsd2docbook xsl takes as input for providing samples in the types section -->

  <type name="announcements">
    <sample src="./samples/types-announcements.xml" title="announcements"> </sample>
  </type>

  <type name="resources">
    <sample src="./samples/types-resources.xml" title="A resources instance using script URIs">
      <annotation>
        <db:para>This example shows a <db:type>resources</db:type> instance where the <db:code>uri</db:code> attribute
          refers to a script that does not expose the names of the resources.
        </db:para>
      </annotation>
    </sample>
  </type>

  <type name="contentList">
    <sample src="./samples/types-contentList-1.xml" title="A complete contentList with audio labels">
      <annotation>
        <db:para>This example shows a complete <db:type>contentList</db:type> (the 
          <db:code>firstItem</db:code> and <db:code>lastItem</db:code> attributes
          are not present on the root element).</db:para>
      </annotation>
    </sample>
    <sample src="./samples/types-contentList-2.xml" title="A partial contentList">
      <annotation>
          <db:para>
          This example shows a partial <db:type>contentList</db:type> (<db:code>firstItem</db:code> and <db:code>lastItem</db:code> attributes
          specify which of the items are included in this segment).</db:para>
      </annotation>
    </sample>
  </type>

  <type name="contentMetadata">
    <sample src="./samples/types-contentMetadata.xml" title="Example contentMetadata">
      <annotation>
        <db:para>This example shows the <db:type>contentMetadata</db:type> for a borrowed book
          that is <db:xref linkend="bib_pdtb2"/> protected (see <db:xref linkend="fundamentals_content_issuing_pdtb"/>). </db:para>
      </annotation>
    </sample>
  </type>

  <type name="readingSystemAttributes">
    <sample src="./samples/types-readingSystemAttributes-1.xml"
      title="readingSystemAttributes for a 
      portable audio-centric Reading System">
      <annotation>
        <db:para>This example shows <db:type>readingSystemAttributes</db:type> for a
          small portabe audio-centric <db:xref linkend="term_readingSystem"/>
          (single format support, numeric input, no text-to-speech capabilities, <db:abbrev>PDTB2</db:abbrev> support).
        </db:para>
      </annotation>
    </sample>
    
    <sample src="./samples/types-readingSystemAttributes-2.xml"
      title="readingSystemAttributes for a more functional
       Reading System">
      <annotation>
        <db:para>This example shows the <db:type>readingSystemAttributes</db:type> for a
          more functional <db:xref linkend="term_readingSystem"/>, such as a software <db:xref linkend="term_readingSystem"/>
          (multi-format support, full keyboard input, built-in text-to-speech capabilities but in this case no 
          <db:abbrev>PDTB2</db:abbrev> support).
        </db:para>
      </annotation>
    </sample>
  </type>

  <type name="serviceAttributes">
    <sample src="./samples/types-serviceAttributes-2.xml"
      title="serviceAttributes for a minimal library Service">
      <annotation>
        <db:para>This example shows the <db:type>serviceAttributes</db:type> for a <db:xref linkend="term_service"/> that only
          supports the <db:link linkend="fundamentals_content_selection_outofband">out-of-band</db:link> Content selection method.</db:para>
      </annotation>
    </sample>
    <sample src="./samples/types-serviceAttributes-1.xml"
      title="serviceAttributes for a fuller Service">
      <annotation>
        <db:para>This example shows the <db:type>serviceAttributes</db:type> for a <db:xref linkend="term_service"/> that supports
          both <db:xref linkend="fundamentals_content_selection"/> and provides some or all
          of its <db:xref linkend="term_content"></db:xref> protected using <db:xref linkend="bib_pdtb2"/>.</db:para>
      </annotation>
    </sample>
  </type>

</samples>
